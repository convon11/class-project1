mstimesone=print(import"the time it will happen is:")
mstimestwo=print(import"the time it will happen is:")
mstimesthree=print(import"the time it will happen is:")

import time()
t=time()
from Tkinter import*
def scedual():
  print(import"the first item on the scedual is:")
  print(mstimesone)
  print(import"the second item on the scedual is:")
  print(mstimestwo)
  print(import"the third item on the scedual is:")
  print(mstimesthree)

tk=Tkinter()
create_scedual=Tkinter.Button(tk,text="create scedual",command=scedual)
create_scedual.pack
tk.mainloop()
