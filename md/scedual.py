import time()
t=time()
from Tkinter import*
def scedual():
  print(import"the first item on the scedual is:")
  print(import"the time it will happen is:")

tk=Tkinter()
create_scedual=Tkinter.Button(tk,text="create scedual",command=scedual)
create_scedual.pack
tk.mainloop()
